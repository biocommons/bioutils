[pytest]
doctest_optionflags = ALLOW_UNICODE ALLOW_BYTES ELLIPSIS NORMALIZE_WHITESPACE
addopts = --cov-config=.coveragerc --cov-report term-missing --doctest-modules --doctest-glob='*.rst' --strict
collect_ignore = setup.py
norecursedirs = _data
testpaths = tests
markers =
  network
  
# --addopts="--cov=${PKG} ${PKG} tests"
